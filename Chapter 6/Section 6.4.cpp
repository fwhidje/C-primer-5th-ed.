#include <iostream>
using namespace::std;

//Exercise 6.39
/* 
(a) Illegal: there is no distinction between parameters with and without top-level const.
A solution would be to use references or pointers to const int.
int calc(int, int);
int calc(const int, const int);

(b) Illegal: functions cannot be overloaded if they only differ in return-type.
int get();
double get();

(c) Legal
int *reset(int *);
double *reset(double *);
*/



int main() {

	return 0;
}